# ── Core ML ───────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
grad-cam==1.5.5

# ── Web framework ─────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1

# ── Database / storage ────────────────────────────────────
supabase==2.10.0

# ── CrewAI (versions tested together) ─────────────────────
crewai==0.86.0
crewai-tools==0.17.0
litellm==1.56.4
openai==1.59.7
instructor==1.7.2

# ── LangChain family (CrewAI 0.86-era versions) ──────────
langchain==0.3.14
langchain-community==0.3.14
langchain-openai==0.2.14
langchain-core==0.3.29
langchain-text-splitters==0.3.5

# ── RAGAS ─────────────────────────────────────────────────
ragas==0.2.10
datasets==3.2.0

# ── Auth & email & scheduling ─────────────────────────────
bcrypt==4.2.1
passlib==1.7.4
PyJWT==2.10.1
resend==2.5.1
APScheduler==3.10.4
pydantic[email]   # ← add this line
markdown==3.7   # ← add this line

# ── Image / PDF / HTTP ────────────────────────────────────
pillow==11.0.0
xhtml2pdf==0.2.17   # clinical report PDF generation (base64 <img> support)
opencv-python==4.10.0.84
requests==2.32.3
PyYAML==6.0.2